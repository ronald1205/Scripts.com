<!DOCTYPE html> <html lang="es"> <head>   <meta charset="UTF-8">   <meta name="viewport" content="width=device-width, initial-scale=1.0">   <title>Mi Página de Scripts</title>   <style>     /* Estilos generales */     body, html {       margin: 0;       padding: 0;       height: 100%;       overflow: hidden;       font-family: Arial, sans-serif;       background: black;       color: white;       display: flex;       flex-direction: column;       justify-content: center;       align-items: center;     }      /* Lienzo para las bolitas */     canvas {       position: absolute;       top: 0;       left: 0;       width: 100%;       height: 100%;       z-index: -1;     }      /* Botón */     .btn {       background: red;       color: white;       border: none;       padding: 15px 40px;       font-size: 20px;       font-weight: bold;       border-radius: 50px; /* redondeado */       cursor: pointer;       transition: transform 0.2s ease, background 0.3s;     }      .btn:hover {       background: darkred;       transform: scale(1.05);     }      /* Texto debajo */     .text {       margin-top: 15px;       font-size: 24px;       font-weight: bold;       color: #ff4444;     }   </style> </head> <body>   <!-- Fondo animado -->   <canvas id="bg"></canvas>    <!-- Contenido principal -->   <button class="btn" id="copyBtn">Get Script</button>   <div class="text">🔥PRIVATE SERVER🔥</div>    <script>     // ----------- ANIMACIÓN DE BOLITAS ------------     const canvas = document.getElementById("bg");     const ctx = canvas.getContext("2d");     let balls = [];      function resizeCanvas() {       canvas.width = window.innerWidth;       canvas.height = window.innerHeight;     }     window.addEventListener("resize", resizeCanvas);     resizeCanvas();      for (let i = 0; i < 50; i++) {       balls.push({         x: Math.random() * canvas.width,         y: Math.random() * canvas.height,         r: Math.random() * 5 + 2,         dx: (Math.random() - 0.5) * 1.2,         dy: (Math.random() - 0.5) * 1.2,       });     }      function animate() {       ctx.clearRect(0, 0, canvas.width, canvas.height);       ctx.fillStyle = "white";        balls.forEach((b) => {         ctx.beginPath();         ctx.arc(b.x, b.y, b.r, 0, Math.PI * 2);         ctx.fill();          b.x += b.dx;         b.y += b.dy;          if (b.x < 0 || b.x > canvas.width) b.dx *= -1;         if (b.y < 0 || b.y > canvas.height) b.dy *= -1;       });        requestAnimationFrame(animate);     }      animate();      // ----------- BOTÓN DE COPIAR SCRIPT ------------     const copyBtn = document.getElementById("copyBtn");      // Tu script real     const scriptCode = `loadstring(game:HttpGet("https://pastebin.com/raw/Ru4UQDpN"))()`;      copyBtn.addEventListener("click", () => {       navigator.clipboard.writeText(scriptCode).then(() => {         // Cambiar botón y título         copyBtn.innerText = "COPIED📝";         copyBtn.style.background = "green";         document.titl
